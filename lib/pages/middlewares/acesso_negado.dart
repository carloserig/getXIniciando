import 'package:flutter/material.dart';
    
class AcessoNegado extends StatelessWidget {

  const AcessoNegado({ Key? key }) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Acesso Negado'),
      ),
      body: Container(),
    );
  }
}